package com.family.tree.render

import com.family.tree.layout.LayoutResult

/**
 * Routes edges between nodes; placeholder for orthogonal routing.
 */
class EdgeRouter {
    fun routeEdges(layout: LayoutResult) {
        // No-op for now; can compute connection ports and segments if needed.
    }
}
