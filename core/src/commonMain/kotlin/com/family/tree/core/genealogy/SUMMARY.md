# Интеграция с генеалогическими API - Краткое резюме

## ✅ Что было создано

Полная архитектура для интеграции с генеалогическими API (FamilySearch, Ancestry, MyHeritage и др.) с поддержкой AI-сопоставления персон.

## 📁 Структура файлов

```
core/src/commonMain/kotlin/com/family/tree/core/genealogy/
│
├── 📄 GenealogyApiConfig.kt           # Конфигурация провайдеров API
├── 📄 GenealogyApiService.kt          # Основной сервис-координатор
├── 📖 README.md                        # Подробная документация
├── 📖 ARCHITECTURE.md                  # Архитектурный документ
│
├── 📂 client/                          # Клиенты для работы с API
│   ├── GenealogyApiClient.kt          # Базовый интерфейс + абстрактный класс
│   └── FamilySearchClient.kt          # Реализация для FamilySearch API
│
├── 📂 model/                           # Модели данных
│   └── ExternalPerson.kt              # Унифицированная модель персоны
│                                       # + ExternalDate, ExternalPlace, ExternalEvent
│                                       # + PersonSearchCriteria, PersonSearchResult
│
├── 📂 parser/                          # Парсеры форматов данных
│   └── GenealogyDataParser.kt         # GedcomXParser, Gedcom55Parser
│
├── 📂 matching/                        # Сопоставление персон
│   ├── PersonMatcher.kt               # Базовое сопоставление (Levenshtein)
│   └── AiPersonMatcher.kt             # AI-powered сопоставление (LLM)
│
└── 📂 example/                         # Примеры использования
    └── GenealogySearchExample.kt      # 5 примеров различных сценариев
```

## 🎯 Ключевые возможности

### 1. **Поддержка множественных провайдеров**
- ✅ FamilySearch (базовая реализация)
- 🔄 Ancestry (заглушка)
- 🔄 MyHeritage (заглушка)
- 🔄 WikiTree (заглушка)
- ✅ Легко добавить новые провайдеры

### 2. **Парсинг различных форматов**
- ✅ GEDCOM X (JSON) - базовая реализация
- 🔄 GEDCOM 5.5 (текстовый) - заглушка
- ✅ Расширяемая система парсеров

### 3. **Интеллектуальное сопоставление**
- ✅ **BasicPersonMatcher**: алгоритм Левенштейна для нечёткого сравнения
- ✅ **AiPersonMatcher**: использует уже подключённые LLM для:
  - Учёта культурных особенностей
  - Распознавания транслитерации
  - Понимания исторического контекста
  - Предоставления объяснений и предложений

### 4. **Унифицированная модель данных**
- `ExternalPerson` - промежуточное представление
- Поддержка дат, мест, событий, родственных связей
- Метаданные и источники информации

## 🚀 Быстрый старт

```kotlin
// 1. Создание сервиса
val genealogyService = GenealogyApiServiceBuilder()
    .withHttpClient(httpClient)
    .withAiClient(aiClient, aiConfig)
    .addProvider("familysearch", GenealogyApiConfig(
        provider = "FAMILYSEARCH",
        apiKey = "your-api-key",
        enabled = true
    ))
    .build()

// 2. Поиск персоны
val criteria = PersonSearchCriteria(
    firstName = "Иван",
    lastName = "Иванов",
    birthYear = 1850
)
val results = genealogyService.searchInAllProviders(criteria)

// 3. Сопоставление с локальной персоной
val matches = genealogyService.findMatchesForPerson(
    localPerson = myPerson,
    matchingConfig = PersonMatchingConfig(
        useAiMatching = true,  // Использовать AI
        minConfidenceScore = 0.7
    )
)
```

## 🏗️ Архитектурные принципы

1. **Модульность**: каждый компонент отвечает за свою задачу
2. **Расширяемость**: легко добавлять новые провайдеры и форматы
3. **Интеграция**: использует существующие `AiClient`, `Individual`, `HttpClient`
4. **Гибкость**: поддержка различных стратегий сопоставления
5. **Безопасность**: защита API ключей, управление rate limiting

## 📊 Workflow

```
User → GenealogyApiService
         ↓
    ┌────┴────┐
    ↓         ↓
API Client  PersonMatcher
    ↓         ↓
External    LLM (AI)
  API
    ↓
  Parser
    ↓
ExternalPerson → PersonMatchResult
```

## 🔧 Интеграция с существующим кодом

### Используемые компоненты:
- ✅ `AiClient` и `AiConfig` - для AI-сопоставления
- ✅ `Individual` - модель локальной персоны
- ✅ `HttpClient` (Ktor) - для HTTP запросов
- ✅ Существующая инфраструктура сериализации

### Новые компоненты:
- `GenealogyApiService` - основной сервис
- `ExternalPerson` - модель внешней персоны
- `PersonMatchResult` - результат сопоставления

## 📝 Примеры использования

В файле `example/GenealogySearchExample.kt` представлены 5 сценариев:

1. **Простой поиск** - поиск по имени и году рождения
2. **AI-сопоставление** - интеллектуальный поиск совпадений
3. **Детальное сравнение** - сравнение двух конкретных персон
4. **Проверка подключения** - тестирование API и лимитов
5. **Массовый поиск** - обработка списка персон

## 🎨 Особенности AI-сопоставления

### Промпт для LLM включает:
- Все доступные данные о персонах
- Инструкции по учёту культурных особенностей
- Запрос на объяснение решения
- Формат ответа (JSON)

### Ответ LLM содержит:
- `confidenceScore` - уверенность (0.0-1.0)
- `matchType` - тип совпадения (EXACT, HIGH, MEDIUM, LOW, NO_MATCH)
- `matchedFields` - совпавшие поля с оценкой схожести
- `conflicts` - конфликты с предложениями по разрешению
- `explanation` - подробное объяснение
- `suggestions` - рекомендации по улучшению данных

## 🔐 Безопасность

- API ключи хранятся в конфигурации
- HTTPS для всех запросов
- Управление rate limiting
- Локальные данные не отправляются без согласия

## 📈 Roadmap

### MVP (Текущая фаза) ✅
- Базовая архитектура
- FamilySearch клиент
- GEDCOM X парсер
- Базовое и AI сопоставление
- Документация

### Фаза 2 🔄
- Полная реализация GEDCOM X
- GEDCOM 5.5 парсер
- Дополнительные провайдеры
- Кэширование

### Фаза 3 🔮
- Batch операции
- Автоматическое обогащение данных
- Визуализация
- UI интеграция

## 📚 Документация

- **README.md** - подробное руководство пользователя
- **ARCHITECTURE.md** - архитектурный документ с диаграммами
- **GenealogySearchExample.kt** - примеры кода

## 🎯 Следующие шаги

1. **Получить API ключи**:
   - FamilySearch: https://www.familysearch.org/developers/
   - Другие провайдеры по мере необходимости

2. **Протестировать базовый поиск**:
   ```kotlin
   val example = GenealogySearchExample(httpClient, aiClient, aiConfig)
   example.initialize(familySearchApiKey = "your-key")
   example.simpleSearch()
   ```

3. **Попробовать AI-сопоставление**:
   ```kotlin
   example.findMatchesWithAi()
   ```

4. **Расширить функциональность**:
   - Доработать GEDCOM X парсер
   - Добавить новые провайдеры
   - Реализовать кэширование

## 💡 Преимущества решения

1. **Гибкость**: работа с любыми генеалогическими API
2. **Интеллектуальность**: AI понимает контекст и особенности
3. **Расширяемость**: легко добавлять новые источники
4. **Интеграция**: использует существующую инфраструктуру
5. **Производительность**: параллельный поиск в нескольких источниках
6. **Качество**: детальная оценка совпадений с объяснениями

## 🤝 Вклад в проект

Архитектура готова к использованию и дальнейшему развитию. Основные компоненты реализованы, документация подготовлена, примеры созданы.

**Готово к интеграции в основное приложение!** 🎉
